# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String!
  refreshToken: String!
  user: User!
}

input CreateProjectInput {
  description: String
  name: String!
}

type DashboardStats {
  activeSessions: Int!
  activeSessionsChange: Float!
  avgLatency: String!
  avgLatencyChange: Float!
  errorRate: Float!
  errorRateChange: Float!
  eventFrequency: [Int!]!
  totalEvents: Int!
  totalEventsChange: Float!
  trend: [TrendData!]!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DebugEvent {
  arguments: String
  childEvents: [DebugEvent!]
  columnNumber: Int
  depth: Int!
  duration: Int
  errorMessage: String
  errorStack: String
  filePath: String
  httpMethod: String
  httpStatus: Int
  httpUrl: String
  id: ID!
  lineNumber: Int
  name: String
  parentEvent: DebugEvent
  parentEventId: String
  returnValue: String
  session: DebugSession!
  sessionId: String!
  timestamp: DateTime!
  type: String!
}

type DebugSession {
  endedAt: DateTime
  environment: String!
  events: [DebugEvent!]
  id: ID!
  ipAddress: String
  metadata: String
  project: Project!
  projectId: String!
  startedAt: DateTime!
  userAgent: String
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  createProject(input: CreateProjectInput!): Project!
  deleteProject(id: ID!): Project!
  deleteSession(id: ID!): DebugSession!
  login(input: LoginInput!): AuthResponse!
  logout(input: RefreshTokenInput!): Boolean!
  refreshToken(input: RefreshTokenInput!): AuthResponse!
  regenerateProjectKey(id: ID!): Project!
  register(input: RegisterInput!): AuthResponse!
  updateProject(id: ID!, input: UpdateProjectInput!): Project!
}

type PaginatedEvents {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  items: [DebugEvent!]!
  limit: Int!
  page: Int!
  totalCount: Int!
  totalPages: Int!
}

type PaginatedProjects {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  items: [Project!]!
  limit: Int!
  page: Int!
  totalCount: Int!
  totalPages: Int!
}

type PaginatedSessions {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  items: [DebugSession!]!
  limit: Int!
  page: Int!
  totalCount: Int!
  totalPages: Int!
}

type Project {
  activityTrend: [Float!]
  apiKey: String!
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!
  storageLimit: String
  storageUsage: String!
  updatedAt: DateTime!
  user: User!
}

type Query {
  dashboardStats(range: String): DashboardStats!
  getHello: String!
  me: User!
  project(id: ID!): Project!
  projectStats(projectId: ID!, range: String): DashboardStats!
  projects(limit: Int! = 10, page: Int! = 1): PaginatedProjects!
  recentSessions(limit: Int! = 5, page: Int! = 1): PaginatedSessions!
  session(id: ID!): DebugSession!
  sessionEvents(limit: Int! = 10, page: Int! = 1, sessionId: ID!): PaginatedEvents!
  sessions(limit: Int! = 10, page: Int! = 1, projectId: ID!): PaginatedSessions!
}

input RefreshTokenInput {
  refreshToken: String!
}

input RegisterInput {
  email: String!
  name: String
  password: String!
}

type TrendData {
  hour: String!
  requests: Int!
}

input UpdateProjectInput {
  description: String
  name: String
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String
  plan: String!
  updatedAt: DateTime!
}
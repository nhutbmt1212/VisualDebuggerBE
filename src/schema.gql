# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type AuthResponse {
  accessToken: String!
  user: User!
}

input CreateProjectInput {
  description: String
  name: String!
}

type DashboardStats {
  activeSessions: Int!
  avgLatency: String!
  errorRate: Float!
  eventFrequency: [Int!]!
  totalEvents: Int!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

type DebugEvent {
  arguments: String
  childEvents: [DebugEvent!]
  columnNumber: Int
  depth: Int!
  duration: Int
  errorMessage: String
  errorStack: String
  filePath: String
  httpMethod: String
  httpStatus: Int
  httpUrl: String
  id: ID!
  lineNumber: Int
  name: String
  parentEvent: DebugEvent
  parentEventId: String
  returnValue: String
  session: DebugSession!
  sessionId: String!
  timestamp: DateTime!
  type: String!
}

type DebugSession {
  endedAt: DateTime
  environment: String!
  events: [DebugEvent!]
  id: ID!
  ipAddress: String
  metadata: String
  project: Project!
  projectId: String!
  startedAt: DateTime!
  userAgent: String
}

input LoginInput {
  email: String!
  password: String!
}

type Mutation {
  createProject(input: CreateProjectInput!): Project!
  deleteProject(id: ID!): Project!
  deleteSession(id: ID!): DebugSession!
  login(input: LoginInput!): AuthResponse!
  regenerateProjectKey(id: ID!): Project!
  register(input: RegisterInput!): AuthResponse!
  updateProject(id: ID!, input: UpdateProjectInput!): Project!
}

type PaginatedProjects {
  hasNextPage: Boolean!
  hasPreviousPage: Boolean!
  items: [Project!]!
  limit: Int!
  page: Int!
  totalCount: Int!
  totalPages: Int!
}

type Project {
  apiKey: String!
  createdAt: DateTime!
  description: String
  id: ID!
  name: String!
  updatedAt: DateTime!
  user: User!
  userId: String!
}

type Query {
  dashboardStats: DashboardStats!
  getHello: String!
  me: User!
  project(id: ID!): Project!
  projects(limit: Int! = 10, page: Int! = 1): PaginatedProjects!
  recentSessions(limit: Int! = 5): [DebugSession!]!
  session(id: ID!): DebugSession!
  sessions(projectId: ID!): [DebugSession!]!
}

input RegisterInput {
  email: String!
  name: String
  password: String!
}

input UpdateProjectInput {
  description: String
  name: String
}

type User {
  createdAt: DateTime!
  email: String!
  id: ID!
  name: String
  updatedAt: DateTime!
}